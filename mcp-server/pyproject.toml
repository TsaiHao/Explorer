[project]
name = "mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastmcp>=2.14.5",
    "httpx>=0.28.1",
    "jsonschema>=4.20.0",
]

[project.scripts]
explorer-mcp = "server:main"
